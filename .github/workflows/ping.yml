name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 * * *"  # 10분마다 실행
  workflow_dispatch:        # 수동 실행 가능

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send request to Render app
        run: |
          curl -s https://find-blue.onrender.com > /dev/null
